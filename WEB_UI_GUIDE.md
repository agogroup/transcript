# Web UI ガイド

Whisper文字起こしツールのWeb UIの使い方を説明します。

## 起動方法

### 1. 依存パッケージのインストール

```bash
pip install -r requirements.txt
```

### 2. Web UIの起動

```bash
python run_web.py
```

または

```bash
cd web
python app.py
```

### 3. ブラウザでアクセス

起動後、ブラウザで以下のURLにアクセス：

```
http://localhost:5000
```

## 初期設定

### APIキーの設定

初回起動時は、OpenAI APIキーを設定する必要があります。

1. 画面右上の「設定」をクリック
2. APIキー入力フォームに、OpenAI APIキーを入力
3. 「APIキーを保存」をクリック

APIキーは`.env`ファイルに安全に保存されます。

## 使い方

### 基本的な使い方

1. **ファイルアップロード**
   - ドラッグ&ドロップ、またはクリックして音声ファイルを選択
   - 対応形式: MP3, WAV, M4A, OGG, FLAC, AAC, WMA
   - 最大ファイルサイズ: 500MB

2. **オプション設定**
   - **言語**: 文字起こし対象の言語を選択（自動検出も可能）
   - **出力形式**: テキスト、JSON、SRT字幕、VTT字幕から選択
   - **前処理**: ノイズ除去や音量正規化を有効化

3. **カスタムプロンプト（任意）**
   - **文脈・背景情報**: 会話の内容やシーンを指定
   - **専門用語**: カンマ区切りで専門用語を指定
   - **話者情報**: 話者の人数や役割を指定

4. **文字起こし実行**
   - 「文字起こしを開始」ボタンをクリック
   - 処理が完了するまで待機（数秒〜数分）

5. **結果のダウンロード**
   - 完了後、プレビューを確認
   - 「ダウンロード」ボタンで結果をダウンロード

## 機能詳細

### 前処理オプション

- **ノイズ除去**: FFTベースの高度なノイズリダクション
- **音量正規化**: 最適な音量レベルへの自動調整

これらの機能により、文字起こしの精度が向上します。

### 出力形式

- **テキスト (.txt)**: 純粋なテキスト形式
- **JSON (タイムスタンプ付き)**: セグメント情報付きJSON
- **SRT字幕**: 動画字幕用（広く対応）
- **VTT字幕**: Web動画用

### カスタムプロンプトの活用

専門用語や文脈情報を指定することで、文字起こしの精度を向上できます。

**例:**
- 文脈: "医療カンファレンス"
- 専門用語: "カテーテル, 心房細動, 抗凝固薬"
- 話者: "医師3名"

## トラブルシューティング

### ファイルアップロードに失敗する

- ファイル形式が対応しているか確認
- ファイルサイズが500MB以下か確認
- ブラウザのキャッシュをクリアしてリトライ

### 文字起こしが失敗する

- APIキーが正しく設定されているか確認
- OpenAI APIの利用制限を確認
- ネットワーク接続を確認

### 処理に時間がかかる

- 長い音声ファイルは処理に時間がかかります
- 前処理を無効化すると高速化できますが、精度が低下する可能性があります

## ポートの変更

デフォルトではポート5000で起動します。変更する場合は：

`web/app.py`の最終行を編集：

```python
app.run(debug=True, host='0.0.0.0', port=8080)  # ポートを8080に変更
```

または`run_web.py`を編集：

```python
app.run(debug=True, host='0.0.0.0', port=8080)
```

## セキュリティ

- APIキーは`.env`ファイルに保存され、Gitには含まれません
- アップロードファイルは`uploads/`ディレクトリに一時保存されます
- 処理完了後、必要に応じて手動で削除してください

## 本番環境での運用

本番環境で使用する場合は、以下の設定を推奨します：

### 1. Gunicornの使用

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 web.app:app
```

### 2. Nginxのリバースプロキシ

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. HTTPSの設定

Let's Encryptなどを使用してSSL証明書を取得し、HTTPSを有効化してください。

## 開発モード

開発時は、`debug=True`で起動すると、コード変更時に自動的にリロードされます（デフォルト設定）。

本番環境では必ず`debug=False`に設定してください。

---

問題が発生した場合は、GitHubのissueを作成してください。
